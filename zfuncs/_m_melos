#autoload

function _m_melos() {
    local root="$(_m_find_root)" || {
        print -ru2 -- "Not inside a Melos workspace (no melos.yaml or pubspec workspace config found)."
        return 1
    }
    (cd "$root" && command melos "$@")
}
