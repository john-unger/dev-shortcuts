#autoload

function g-module() {
    local module="${1:-}"

    if [[ -z "$module" ]]; then
        echo "Usage: g-module <module>" >&2
        return 1
    fi

    echo -e "Running \e[34m./gradlew \e[32m-p $module \e[35minstall\e[0m"
    ./gradlew -p "$module" install
}
